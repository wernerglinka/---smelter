/**
 * @deprecated Use import { formatJson } from '@utils/file/json' instead
 * 
 * This file provides backward compatibility during the transition to the new
 * utilities organization. It re-exports functionality from the new location.
 * 
 * @module file-ops/render-json-file
 */

import { formatJson as renderJSONFile } from '@utils/file/json';

export { renderJSONFile };
